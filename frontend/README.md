# JobTracker web

Next.js 15 App Router UI for JobTracker. See the repository root `README.md` for setup.

```bash
npm install
npm run dev    # http://localhost:3000
npm test       # Vitest
npm run e2e    # Playwright (API must be running)
```
